services:
  - type: web
    name: french-vocabulary-mcq
    runtime: node
    rootDir: Day1/french-vocabulary-mcq
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
